# Tests — Acme Co. BU

Test organization as the Suite gets built:

- `agents/` — per-agent prompt tests (input → expected output, eval rubrics).
- `integration/` — multi-agent handoff tests across layers.
- `e2e/` — full cycle tests (onboarding → campaign launch → analytics).

Start populating in Phase 1 Week 2+ as Agent 1.1 comes online. Framework choice depends on S0-T1.
